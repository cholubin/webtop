<h1>Listing mybookpdfs</h1>

<table>
  <tr>
  </tr>

<% @mybookpdfs.each do |mybookpdf| %>
  <tr>
    <td><%= link_to 'Show', mybookpdf %></td>
    <td><%= link_to 'Edit', edit_mybookpdf_path(mybookpdf) %></td>
    <td><%= link_to 'Destroy', mybookpdf, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New mybookpdf', new_mybookpdf_path %>